// randomContract: yes - random contract gets deployed, no - empty '0x' contract
export const randomContract = 'yes'

// shuffle: yes - private keys gets shuffled, no - they don't
export const shuffle = 'yes'

// Max Gwei with which code gets executed
export const maxGas = '15'

// Sleep: range of seconds to sleep between each iteration.
export const pause = [20, 40]
